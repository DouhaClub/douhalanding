Textura das barras da lista "Sets Unicos" (Home e /sets) — cada linha, nao o fundo da secao.

Arquivo atual: sets-topics-texture.svg (gerado no codigo, tom terracota).

Para usar exatamente a imagem que voce exportou do design:
1. Salve como sets-topics-texture.jpg (ou .png) nesta pasta
2. Em src/index.css, na regra .sets-topics-block--textured .sets-topic-row,
   troque a URL para url('/brand/textures/sets-topics-texture.jpg')
